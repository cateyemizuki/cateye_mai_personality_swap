# 夺舍麦麦 开发文档

> 版本 1.0.0（2026-09-06）　适用 MaiBot 1.2.x / maibot_sdk 2.x
> 覆盖插件版本至 **v1.4.0**（2026-09-07：v1.3.7 脚本侧新增持久 KV `ctx.kv_*`、
> @/bot 身份 `ctx.at_*` / `ctx.bot_*` / `ctx.is_at_me`；v1.3.8 引入"当前身份提醒"
> 尾注；v1.3.9 planner 外壳 {bot_name} 随 persona_name 人格化（§4.3）；v1.4.0
> 定稿身份说明文案并让 planner 改写宿主末位提醒（§4.2））。
> 前身：persona_style_injector（注入入口原型），本项目在其通道架构上
> 演进为完整的"预设 + 替换状态 + 自动触发"系统。

---

## 1. 功能总览

- **预设**：把官方 bot_config 当前人格（`personality.personality` /
  `reply_style` / `behavior_style`）一键保存为预设文件；预设含
  名称 / 注入表达 / 人格约束 / 行为风格 / **默认切换时长**。
- **切换**：命令手动切换（当前聊天流或全局）+ 条件/概率/权重自动切换；
  每次切换的生效时长 = 显式声明优先、否则回退预设记录的默认时长；到时自动
  恢复主人格（时长 0 = 一直用不自动恢复）。**预设文件本身不过期**。
- **注入**：激活预设期间，表达+人格注入 replyer、行为风格注入 planner；
  同时按配置从请求中剔除官方"临时说话风格"消息。
- **持久化**：替换状态落盘，重启后依旧知道"哪个群换成了哪个人格、还剩多久"。

## 2. 代码结构

```
cateye_mai_personality_swap/
├── _manifest.json        # id = github.cateye.mai-personality-swap，作者 cateye
├── plugin.py             # 配置模型（config_model）+ 5 个 Hook + /mps、/maisave 命令
├── preset_store.py       # 预设 TOML 存取 + 备份轮转
├── swap_state.py         # 状态/权重覆盖持久化
├── swap_engine.py        # 条件/概率/权重抽取（纯函数，可单测）
├── AGENT.md              # 面向 AI 编码助手的 maips 脚本编写契约
├── 自检指南.md           # maips 全方法自检步骤（配合 maips/maips_selfcheck.py）
├── maips/                # 自定义脚本目录（KubeJS 的 kubejs/ 同构，可放多个 .py）
│   ├── example.py        # 示例脚本（含按群/私聊路由人格的可用代码）
│   └── maips_selfcheck.py # 方法自检脚本（/mps debug true 时响应）
└── injectors/
    ├── base.py           # 提示块渲染 + ContextItem 快照构造 + 临时风格剔除
    ├── preset_blocks.py  # 从预设生成三个注入块
    └── system_replace.py # 覆盖式注入：按宿主模板锚点切分改写 system 的官方人设段
```

**配置不随包附带 config.toml**（开发文档《02-开发入门/03-配置系统》的规范）：
插件声明 `config_model`（全部字段有默认值 + 中文描述），Runner 加载时自动生成
默认配置、补齐缺失字段并增量写回插件目录的 `config.toml`（tomlkit 合并，尽量
保留用户注释）；修改走 WebUI 插件配置页。`config_version` 为 1.2.3 硬性要求，
由 `SUPPORTED_CONFIG_VERSION` 常量与 manifest version 同步，UI 中 hidden+disabled。
黑白名单的模式字段（`group_list_mode` / `private_list_mode`）用 `Literal[...]`
声明，WebUI 渲染为下拉选择。**注入方式无配置项**——恒为覆盖式 system 改写。

## 3. 数据格式

### 3.0 路径规则（易误读点）

宿主统一分配插件数据目录：`build_plugin_paths(plugin_id, project_root)`
（`src/plugin_runtime/runner/plugin_paths.py`）→
**`<MaiBot根>\data\plugins\<manifest插件id>\`**。两点注意：

1. 子目录名是 **manifest 的插件 id**，不是插件目录名。本插件 id 为
   `github.cateye.mai-personality-swap`，因此实际数据根目录是
   `E:\maibot\MaiBot\data\plugins\github.cateye.mai-personality-swap\`
   （`data\plugins` 下其余插件也全是 id 命名；曾看到的 `cateye_skland_sign`
   目录是旧版用目录名命名时的历史残留，与新版 `github.cateye.skland.sign` 并存）。
2. 插件 id 必须匹配 `^[A-Za-z0-9_]+(?:[.-][A-Za-z0-9_]+)+$`（至少含一个
   `.`/`-` 分隔符），裸目录名形式的 id 会被宿主拒绝。所以代码里始终用
   `ctx.paths.data_dir`（宿主授予的绝对路径）定位，**不自行拼接**宿主根路径。

以下 3.1/3.2 的 `preset`、`personality` 均相对该数据根目录。

### 3.1 预设文件 `preset/<名称>.toml`

正文是多行长文本，JSON 只能存成 `\n` 转义（可读性差），故用 **TOML 多行
基本字符串**：写侧 `tomlkit.string(text, multiline=True)`，读侧标准库
`tomllib`——文件里就是真实换行，手工可编辑，读取换行原样保留。

```toml
name = "preset1"
expression = """说话轻声细语，句子短。
不用感叹号。"""
persona = """你是……（第二行）"""
behavior = """优先回应@你的消息……"""
duration_minutes = 60
created_at = "2026-09-06T19:00:00+08:00"
```

> **`duration_minutes` 语义**：它是该预设的**默认切换时长**（`/maisave` 第三参或
> 手写字段）——当命令/脚本/自动替换切到该预设且**未显式声明时长**时，本次激活
> 最多持续该分钟数、到点自动恢复主人格；`0` = 一直用、不自动恢复（推荐默认）。
> **它不是过期时间**：预设文件永久存在、永不销毁，任何时刻都能再切到；改字段
> 只影响"将来切换时的默认持续多久"。显式时长（`/mps swap <名> 120` /
> `ctx.swap(名, duration_minutes=120)`）会覆盖文件值，仅影响当次。

- 名称校验 `validate_preset_name`：中文/字母/数字/下划线/连字符，1~64 位（防路径穿越）。
- 同名覆盖保存前备份到 `preset/backup/<名称>_<时间戳>.toml`，
  按 `preset.backup_limit`（默认 5）裁剪最旧。

### 3.2 替换状态 `personality/<键>.json`

- 键：群聊 = 群号；私聊 = QQ 号；全局模式（`swap.per_stream=false`）= `main`。
- **主人格用布尔标记**，不把"主人格"写成预设名（防止与同名预设混淆）：
  ```json
  {"is_main": true}
  ```
  非主人格：
  ```json
  {"is_main": false, "preset": "preset1", "started_at": 1730000000.0,
   "duration_minutes": 60, "expires_at": 1730003600.0, "source": "command"}
  ```
- 非主人格状态文件里的 `duration_minutes` / `expires_at` 是**本次激活**的计时
  （本次切到该预设后何时自动恢复主人格），来自切换时生效的时长（显式或预设默认值）；
  `duration_minutes = 0`（一直用）时 `expires_at` 为 null。**这与预设文件的
  duration_minutes 是"默认值来源"，预设本身不过期、不被本文件影响。**
- 到期判定是**惰性**的：任何 `get()` 都检查 `expires_at`，到期即回写主人格——
  无需定时器，重启后同样正确。
- `weights.json`：`/mps weight` 写入的 `{预设名: 权重}` 覆盖，与配置页中的
  预设权重表（`weights.presets` 字段）合并（命令优先），重启保留。

### 3.3 stream→群号 映射

状态按群号存储，但 Hook/命令拿到的是 `stream_id`。插件维护内存映射
`_stream_key_cache`，三个来源：

1. `on_load` 时 `ctx.chat.get_all_streams()` 预热；
2. `chat.receive.after_process` / `send_service.after_send` 观察到的每条消息
   （`message_info.group_info.group_id` / `user_info.user_id`）持续补充；
3. 命令时映射缺失会再拉一次全量流；仍失败则**兜底用 stream_id 命名**状态文件，
   保证功能可用（注入侧同规则，两端一致）。

## 4. 注入方式（覆盖式注入，唯一注入方式）

预设激活期间，把请求 items **首条 SystemMessageItem** 里的官方人格/表达/行为
段落**改写为预设内容**——官方人设从请求中消失，模型眼里只有预设人设，不存在
"原人格 vs 注入人格"冲突、也不会被当作提示注入而忽视。**无配置开关**，配置触发
（自动替换）与 maips 脚本触发（`ctx.swap`）一律走此路径；主人格（无预设激活）
不改写 system。

| 通道 | Hook | 动作 |
|---|---|---|
| replyer | `maisaka.replyer.before_model_request`（BLOCKING, NORMAL） | 改写首条 system 的 identity 段（名字行+人格）与 reply_style 段为预设 expression/persona；同时剔除官方临时说话风格 |
| planner | `maisaka.planner.before_request`（BLOCKING, **LATE**） | 改写首条 system 的行为风格段为预设 behavior；preset 配 persona_name 时外壳 {bot_name} 一并人格化（v1.3.9，见 4.3） |
| 当前身份说明（v1.4.0） | 同上两 Hook | replyer items 最末追加、planner 改写 host 末位提醒为同一"你现在是{人格名}…"文案（见 4.2） |
| （回退） | 同上两 Hook | 锚点未命中 → items 尾部追加式注入（并入身份尾注） |

### 4.2 当前身份说明（v1.4.0 定稿文案；解决"历史污染"）

人格切换后，请求上下文里往往残留 bot 以旧人格说过的话，模型容易被**自己的
历史发言**带偏、否认新身份（快照实证：replyer system 已是普瑞赛斯，模型仍回
"我是迷迭香呀"）。为此在预设配置了 `persona_name` 时（否则新身份=官方名、无
新旧之分，不注入）注入一条"当前身份"说明，**两通道落地方式不同**：

| 通道 | 落地方式 | 说明 |
|---|---|---|
| replyer | items 最末**追加**一条 UserMessageItem（host replyer 无同类末位提醒，只能追加） | 文本 = `【当前身份提醒】…`（下） |
| planner | **改写** host 每轮追加在 items 末位的提醒（"你需要输出对{官方名}发言的分析，视情况输出文本内容的分析，思考是否进行工具调用"——由宿主 `chat_loop_service._build_planner_final_user_reminder` 构造，hook 触发前已在 items 里） | 找不到该提醒 → 回退追加同文案 |

文案（用户定稿 v1.4.0）：
> 你现在是{persona_name}。上文中{bot_name}的发言是人格切换前你以旧身份
> 说过的，你只需要知道那是你过去进行过的发言，适当参考即可；一切以当前的
> 设定与身份优先。

- **persona_name = 预设 `persona_name` 字段；bot_name = 官方 `bot.nickname`**
  （历史里 bot 旧发言在 planner 以 `user=官方名` 的 Session 消息记录、在 replyer
  以 assistant/带名 user 出现——故"上文中{官方名}的发言"指代准确）。
- 语义取舍：**不否认历史**（明确那些是"自己切换前说过的"，可适当参考语气与
  关系），**锁定当前身份**为新人格名——避免模型把旧发言当成"另一个人"或
  延续旧身份。
- 实现：
  - `injectors/preset_blocks.build_identity_correction_block(persona_name, bot_name)`
    （v1.4.0 起签名；v1.3.8/1.3.9 为 `(preset, display_name)` 旧文案）
  - `injectors/base.rewrite_tail_user_item(items, prefix=..., new_text=...)`
    （改写末位命中项；未命中返回 None）
  - plugin.py 两 hook 组装 items 时调用；锚点未命中的回退追加路径同样并入。

### 4.3 planner 外壳人格化（v1.3.9）

宿主 planner 模板（`maisaka_chat.prompt`）把 `{bot_name}` 用**官方昵称**渲染，
外壳写满官方名——"你需要关注 迷迭香 与用户的对话来为 迷迭香 选择正确的动作
和行为""你不是 迷迭香 本人，不要替 迷迭香 发言""帮迷迭香搜集信息""当你判断
迷迭香现在应该正式发言时调用 reply"等。预设配 `persona_name` 时，覆盖注入
（`injectors/system_replace.replace_planner_system_text`）在替换行为段后把整段
官方昵称**整体替换为 persona_name**，外壳随之人格化（决策模型眼里的 bot 指称
从官方名变人格名）。未配 persona_name → 外壳保持官方昵称（旧行为）。

- 实现要点：行为段替换先完成（生成 `{persona_name}的行为风格：…` marker 行），
  再做全段 `str.replace(官方昵称, persona_name)`；marker 行的官方名也会被一并
  替换，因锚点已消费、不影响二次定位。
- 语义自洽："你不是 {bot_name} 本人，不要替 {bot_name} 发言" 这类**决策者视角**
  句子在换成 persona_name 后依然成立（planner 仍是外部决策者，只是被决策对象
  的指称变了）。
- 注意与 replyer 的区别：replyer identity 名字行自 v1.3.6 随 persona_name；
  planner 外壳人格化是 v1.3.9 新增，两者现在口径一致。

> `maisaka.replyer.before_request`（extra_prompt 通道）已不再用于注入——保留
> 钩子注册但恒 `continue`，避免人格以"用户消息"形式叠在官方 system 之上。

### 4.1 切分锚点与回退

- **切分锚点**（对齐宿主 zh-CN 模板，见 `injectors/system_replace.py`）：
  - replyer system（`maisaka_replyer.prompt` 渲染）：丢弃开头 `identity` 段
    （官方名字行+人格+情绪尾巴，至 `现在请你读读之前的聊天记录` 前），替换为
    「名字行（用 bot 昵称）+ 预设人格」；把固定句 A 行尾 → `你可以参考【回复信息
    参考】` 之间的官方 `reply_style` 段替换为预设表达；固定句 B 起的场景规则
    （群聊/私聊注意块、输出格式指令）原样保留。
  - planner system（`maisaka_chat.prompt` 渲染）：把 `<bot名>的行为风格：…`
    段（至 `\n以上` 固定句）替换为预设 `behavior`，其余保留。
  - 任一锚点未命中（自定义 system / 其它 locale / 其它插件改写 / 异常）→
    **自动回退 items 尾部追加式**注入（replyer / planner 均如此），保证人格仍
    生效、绝不误删或失效；并记 warning 便于排查。

关键实现约束（与宿主契约相关，改动前必读）：

1. 宿主派发器对 blocking 处理器的 `modified_kwargs` **整体替换** kwargs——所有
   handler 必须 `{**kwargs, ...覆盖键}` **全量回传**；命名参数（如
   `item_schema_version`）不会出现在 `**kwargs` 里，需要显式带回。
2. items 是 ContextItem 快照列表；覆盖注入只改写首条 system 的文本 part，
   其余 item/part 原样透传（宿主 `deserialize_prompt_items` 按 item_id 对比，
   仅被改的 system 项失效，其余保留 replay 引用）。锚点未命中回退的追加项是
   新的 UserMessageItem：`item_id` 全局唯一（`mps-<uuid>`）、`logical_turn_id`
   继承末条消息、ISO `timestamp`。
3. 官方临时风格消息的固定文案前缀是
   `你的说话风格可以尝试：`（`maisaka_generator_base._build_temporary_reply_style_message`），
   以 UserMessageItem 形式插在请求 items 里——只能在 `before_model_request`
   剔除，与覆盖注入无关；`suppress_official_temp_style=false` 可关闭剔除。
   建议保持默认（开），避免官方临时说话风格与预设表达并存造成混淆。
4. bot 昵称在 `on_load` 缓存（`_warm_bot_name`，读官方 `bot.nickname`），覆盖
   注入重建身份段名字行时使用；**预设配置 `persona_name` 时以预设名为准**
   （`_preset_display_name`：persona_name → 官方昵称兜底；v1.3.6 起）；读不到昵称
   则只写人格文本、不写名字行。planner 锚点匹配恒用官方昵称定位；预设配
   persona_name 时外壳（v1.3.9）与末位提醒（v1.4.0）会一并人格化（见 4.2/4.3）。

## 5. 自动替换引擎（swap_engine.py）

```
chat.receive.after_process（OBSERVE）
  → 黑/白名单过滤（群/私聊名单为空 = 不过滤；接管时仍生效）
  → 非bot关键词命中 → ConditionTracker 记录（TTL 600s，模块常量）
send_service.after_send（OBSERVE）→ bot关键词命中记录
入站消息（非 notify/command）→ _maybe_auto_swap：
  1) 当前非主人格且 reroll_during_swap=false → 跳过
  2) 时段窗口（支持跨午夜；留空 = 全天）
  3) 已配置的关键词须处于命中 TTL 内
  4) 概率检查（默认 0.1）
  5) 权重抽取：{__main__: weights.main} ∪ 有效预设权重
     （权重表先与实际存在的预设文件求交集，缺文件的条目不参与）
     exclude_current=true 时剔除当前人格 → 必定变更
  6) 抽中 __main__ → 回主人格；抽中预设 → set_preset（未显式时长 → 取预设
     记录的默认切换时长作为本次持续上限，0=一直用；预设本身不过期）
```

**参数来源**：`_effective_swap_params()` 统一裁决——接管模式
（`script.takeover=true`）取脚本运行时参数（`ctx.set_swap_params` 设定，
存于 `ScriptHost.params`，未设定默认"留空"：概率 0、条件空、无候选，即
管线惰性），否则取配置项。`per_stream` / 注入通道 / 临时风格抑制同理有
各自的 `_effective_*` 生效化方法；黑白名单不参与接管，恒以配置为准。

设计取舍（后续可调）：

- 关键词命中 TTL 与"触发一次"的节奏由模块常量 `CONDITION_TTL_SEC = 600` 控制；
  如需按群/按时段细化，把它提为配置项即可。
- 抽中主人格 = 本次不替换（权重语义），不是"必须换出去"；
  "必定变更"由 `exclude_current` 单独控制。
- 自动替换无额外冷却：非主人格期间默认不再触发（reroll 开关），主人格期
  连续触发由概率天然限流；若群内触发过频，可调低 `probability`。

## 6. 命令实现要点

- 单一 `@Command("mps")` + 正则命名组 `rest`，手动分词路由；
  `/maisave` 是独立 Command，`_dispatch(default_sub="maisave")` 时**整个
  rest 都是参数**（`/maisave <名称> [时长]`），仅当首词恰为 `list` 时视为
  列出预设——这是 `/maisave` 与 `/mps maisave` 语义对齐的关键。
- `/mps maisave` 读官方配置走 `ctx.config.get("personality.<字段>")`
  （点路径取叶子字符串，规避整节 pydantic 模型经 RPC 序列化的不确定性；
  返回信封含 `success/value` 时自动解包）。
- 保存/加载回显用 `ctx.send.forward`：节点
  `{"user_id": "0", "nickname": "夺舍麦麦", "segments": [{"type": "text", "content": ...}]}`；
  转发失败自动回退纯文本。

## 7. 脚本系统（KubeJS 风格自定义脚本）

`script_host.py` 实现脚本宿主；用户脚本位于**插件目录下的 `maips/` 文件夹**
（与 KubeJS 的 `kubejs/` 同构）：`*.py` 全部加载、数量不限，`example.py` 为
随插件分发的注释模板，下划线开头的文件视为工具库不加载（可被其他脚本
import）。脚本目录由 plugin.py 自身位置推导（`Path(__file__).parent/maips`）。
脚本通过 `from mps_api import on_event` 订阅事件，实现高度自定义的切换条件。

message/bot_message 事件的表情包情绪来源（宿主自动维护，脚本无需关心）：
`emoji.maisaka.after_select`（bot 发送表情的命中情绪）与
`emoji.register.after_build_description`（表情库 hash→情绪标签索引，
覆盖已标注的表情包）。

### 7.1 事件

| 事件 | 触发时机 | ctx 关键字段 |
|---|---|---|
| `message` | 每条非通知入站消息（含命令，`is_command` 区分） | `text` / `session_id` / `scope_key` / `group_id` / `user_id` / `is_command` |
| `bot_message` | bot 每条发送成功的消息 | `text` / `session_id` |
| `timer` | 定时（`script.tick_interval_sec`，默认 60s，`ge=10`） | `now` / `hour` / `minute`（无默认作用域） |
| `swap` | 任何来源的切换/恢复发生后（auto/command/script） | `scope_key` / `preset`（None=主人格）/ `source` / `duration_minutes` |
| `load` | 脚本加载/重载后（每次 reload 重新触发，用于初始化接管参数） | 无事件数据 |

### 7.2 ctx API

- **切换**：`ctx.swap(预设名, duration_minutes=None, scope=None)`（`duration_minutes`
  省略=None → 取预设记录的默认切换时长作为本次持续上限，`0`=一直用不自动恢复；
  预设文件本身不过期；scope 缺省=事件所属聊天流，timer 事件必须显式 `"global"`）；
  `ctx.revert(scope=...)`；`ctx.current_preset(scope=...)` → 预设名或 None。
- **条件控制**（对内置自动替换管线）：`ctx.veto(reason)` 或处理器返回 `False`
  → 否决本轮；`ctx.set_probability(p)`；`ctx.set_weight(名, 权重)`（名可为主人格
  `"main"`，覆盖本轮抽取池，`weight<=0` 移除）；`ctx.exclude_current_in_round(b)`。
  处理器返回 `dict` 等价：`{"probability", "weights", "exclude_current"}`。
- **精细匹配**：`ctx.segments` / `ctx.message_types` / `ctx.has_type(t)` /
  `ctx.has_text` / `ctx.has_emoji` / `ctx.has_image`；`ctx.matches(pattern, mode)`
  （mode：contains / exact / regex / prefix / suffix，pattern 可传列表任一命中）；
  `ctx.emoji_emotions()`（消息内表情包的情绪标签）、`ctx.match_emoji(emotions, mode)`。
- **@ 与 bot 身份（v1.3.7 起）**：字段 `ctx.bot_user_id` / `ctx.bot_nickname`
  （bot 自身账号与官方昵称，on_load 预热官方配置注入；读不到为空串）；
  `ctx.at_segments`（本消息 `type=="at"` 的原始段）、`ctx.at_targets()`（被 @ 对象
  摘要 `{user_id,nickname,cardname}` 列表）、`ctx.is_at_me`（本消息是否 @ 了 bot：
  at 段账号 == bot_user_id，或文本含 `@官方昵称` 兜底；**不含"@者是否 bot 自己"
  判定**，排除 bot 自 @ 请同时比较 `ctx.user_id != ctx.bot_user_id`）。`at` 段结构
  见 AGENT.md §4/§5.1——段 data 的 `target_user_id` 是被 @ 者数字账号。
- **持久 KV（v1.3.7 起）**：`ctx.kv_get(key, default=None)` / `ctx.kv_set(key, value)`
  （返回 bool）/ `ctx.kv_del(key)`（返回 bool）/ `ctx.kv_keys()`。落盘
  `personality/script_kv.json`，**脚本热重载 / 插件重启保留**——跨事件计数与挂起
  状态用它（模块级变量在重载时清零，见 8 已知限制）。键名仅 `[A-Za-z0-9_.-]` ≤64；
  值须 JSON 可序列化；全表 ≤512 条；所有脚本共享一张表，键加脚本前缀。
- **LLM 调用**（协程）：`await ctx.llm_generate(prompt, task, temperature, max_tokens)`
  （task = 宿主任务名 replyer/planner/utils 等）；`await ctx.llm_json(...)`
  （解析 JSON，自动剥离围栏）；`await ctx.emotion_score(recent_n, prompt_template,
  task, temperature, max_tokens, scale)`（情绪分 0~scale）；`await ctx.recent_context(n)`
  （最近聊天可读文本）；`ctx.get_swap_params()`（生效参数快照）。结构化回填：
  `ctx.set_swap_params(**{k: v for k, v in data.items() if k in mps_api.SWAP_PARAM_KEYS})`。
- **其他**：`ctx.send_text(text)`（协程，需 async 处理器；仅 message/bot_message
  事件）、`ctx.log(msg)`、`ctx.hour/minute/now`。
- 处理器同步/异步皆可；多个处理器按注册顺序串行执行。

### 7.3 与内置管线的关系

```
入站消息 → 派发 message 事件（脚本可 veto / 覆盖 / 直接切换）
         → 内置管线 _maybe_auto_swap：
             脚本 veto → 整体跳过
             probability / exclude_current / 权重池 ← 脚本轮内覆盖
             条件与概率照常 → 抽取
```

脚本想**完全接管**：把 `swap.probability` 配置为 0（内置永不抽取），
全部用 `ctx.swap` 驱动。直接切换与内置管线互不干扰，swap 事件两者都会广播。

### 7.4 加载、热重载与错误隔离

- mtime+size 检测变化（每次 tick 顺带检查、节流 1s）；重载 = 先摘除该文件
  注册的全部处理器再重新 `exec`；文件删除即卸载。
- 单个处理器异常只记 warning（`/mps script list` 可见最后错误），不影响其他
  脚本与 bot 主链路；加载失败的文件保持未生效，修复保存后自动恢复。
- `/mps script list` 查看状态；`/mps script reload` 强制重载。

### 7.5 安全边界（务必知悉）

脚本以**完整 Python 权限**运行在插件 Runner 进程内——与安装一个插件同等信任
级别（这一定位与 KubeJS 相同：脚本属于整合包的受信内容）。只运行自己编写或
审查过的脚本；不要从不可信来源获取脚本文件。

### 7.6 接管模式（script.takeover）

配置节顺序即语义顺序：`[plugin]`（开关+版本号）→ `[filter]` 黑白名单 →
`[script]`（**takeover 接管开关** + 引擎项）→ `[swap]` → `[condition]` →
`[weights]` → `[preset]` → `[compat]`。接管开启后：

- takeover 以下的配置节（swap/condition/weights/preset/compat）**全部按默认值
  处理**；仅插件总开关、配置版本号与黑白名单继续生效（黑白名单节标签注明
  "接管时仍生效"）；
- 脚本引擎强制启用（`script.enabled` 被忽略；tick 间隔与 auto_reload 仍生效）；
- 替换参数由脚本在 `load` 事件经 `ctx.set_swap_params(...)` 设定，存于
  `ScriptHost.params`（默认"留空"：probability 0、条件空、无候选），字段与
  默认值见 `script_host.py::DEFAULT_SWAP_PARAMS`；每次热重载后 load 重触发，
  参数可安全重设；
- 脚本的流级 `ctx.swap/revert` 过黑白名单闸门（`swap_filter_allows`）；
  全局（main）作用域不检查；
- `ctx.swap/revert` 未按预期生效时优先排查：预设是否存在（抛 ValueError）、
  黑白名单是否命中（info 日志"被黑白名单拦截"）。

### 7.7 脚本指南文件

- `AGENT.md`（插件根目录）：面向 AI 编码助手的精确契约（事件表、ctx 方法签名、
  set_swap_params 字段表与默认值、编写守则、完整模板）；
- `自检指南.md`（插件根目录）：maips 全方法自检步骤与预期结果；
- 插件根 `README.md` 已并入面向用户的脚本入门（快速示例 + API 概览 + 接管说明）。
  均为普通 markdown，不会被脚本宿主加载；随插件分发供阅读。

## 8. 已知限制与后续方向

- `reroll_during_swap=true` 时自动重抽可能连续换人格（含换回主人格），
  目前无单独冷却，必要时加"两次替换最小间隔"配置。
- 全局模式（`per_stream=false`）下 `/mps swap` 在任何聊天流执行都是全局生效；
  状态文件固定为 `main.json`。
- 预设不含 `multiple_reply_style`/群私聊提示词等官方其余人格字段；
  需要时扩展 `PersonaPreset` 字段并在 maisave/maiload 中同步读写。
- 入站表情包的情绪匹配仅覆盖表情库中已标注的表情包（hash 查索引）；
  未入库表情包 `emoji_emotions()` 返回空——MaiBot 收集并标注新表情后自动补全索引。
- 切换频率：README 与 maips 指南已建议用户尽量按触发群切人格、必要时才全局切换，
  避免人格切换过频繁导致上下文与人设混乱。
- **脚本模块在热重载 / 插件重启时被整体重建**（模块级变量清零）——跨事件状态
  必须用 `ctx.kv_*`（§7.2）落盘；模块全局只适合本次加载内的一次性缓存。
- **@ 判定不读宿主 is_at/is_mentioned 布尔**：插件观察入站 hook（
  `chat.receive.after_process`）早于宿主 `maisaka` 计算这两个布尔（宿主在
  `_update_message_trigger_state` 才设置），直接读消息 dict 顶层的它们会拿到
  恒 False 的假象；因此 `ctx.is_at_me` 用 at 段账号比对 + 文本 `@昵称` 兜底
  实现（见 §7.2）。
- **`ctx.bot_user_id` 依赖官方 `bot.qq_account`**：未配置/读取失败/多账号平台时
  为空串 → at 段比对自动跳过、退文本兜底；@ 可靠性随之降低（可能漏报用 ID
  @、误报昵称撞车）。需要精确 @ 判定务必配置 `bot.qq_account`。
- KV 表为**所有脚本共享**（`personality/script_kv.json`，≤512 条），键名加脚本
  前缀防冲突；超限写入返回 False。
- WebUI 首页卡片（`@HomeCard`）展示各流状态、预设管理入口，未做。

### 8.1 独立代码审查建议项评估结论（v1.3.2）

对无上下文代码审查报告（30 项）中**建议级**条目的评估结论——已修 / 记录：

| 项 | 内容 | 结论 |
|---|---|---|
| L3 | ScriptContext 暴露 host/backend（观察 API 与执行面同对象） | **不修（记录）**：脚本=受信任内容（等同插件权限），分层沙箱属未来架构重设计，非当前缺陷 |
| L4 | 条件关键词 normalize+判空双处重复 | **已修**：统一 `_keyword_hit` 入口 |
| L5 | 热重载 mtime+size 同秒同长漏检 | **不修（记录）**：概率极低；如需更高灵敏度可改用 mtime_ns |
| L8 | 注入项 logical_turn_id 继承末条 | **不修（记录）**：依赖宿主语义，低风险 |
| L9 | 官方临时风格前缀子串过宽 | **不修（记录）**：宁可漏删不可误删的保守面，方向正确 |
| L10 | LLM 示例脚本无成本防护 | **文档已覆盖**：AGENT.md 守则 5（LLM 成本） |
| L13 | `/mps swap 0` 与时长 0 语义 | **文档已写清**：swap 无参/0=回主人格；命名为 `0` 的预设不可用命令切（已知边界） |
| R1 | 自动替换路径加触发者过滤 | **不修（记录）**：触发者身份过滤是新功能需求，非缺陷；当前按条件+概率语义自洽 |
| R2 | 覆盖注入锚点宿主版本耦合自检 | **已修**：锚点未命中首告警（`_warn_anchor_miss`，replyer/planner 各一次 warning） |
| R3 | 引擎/状态层纯函数单测 | **已补**：见测试资产 `tR_engine_pure.py`（31 断言，跨午夜/权重池/名称校验/TTL） |
| R4 | 时钟/日志抽象重构 | **不修（记录）**：架构重构收益低风险高；当前时间注入已支持测试（各函数可传 now） |
| R5 | items 每请求全量浅拷贝 | **不修（记录）**：当前规模（几十~几百 items）可接受；N 极大时再优化 |
