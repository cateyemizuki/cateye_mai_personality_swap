# maips 自检指南 —— 验证插件/脚本/覆盖注入是否工作

> 配套文件（均已部署到插件目录 / 数据目录）：
> - 预设：`test`、`p_cheerful`、`p_grumpy`、`p_quiet`（数据目录 `preset/*.toml`）
> - 自检脚本：插件目录 `maips/maips_selfcheck.py`（仅在 debug 开启时响应）
> - 示例脚本：插件目录 `maips/example.py`（含按群/私聊路由人格的可用示例）
>
> 保存/新增脚本后自动热重载；`/mps script list` 查看加载状态，`/mps script reload`
> 手动重载。

---

## 0. 快速体检（30 秒）

| 步骤 | 做什么 | 预期 |
|---|---|---|
| 1 | 群内发 `/mps script list` | 返回脚本列表，含 `maips_selfcheck.py` 与 `example.py`，均「已加载」，无 ❌ |
| 2 | 发 `/mps maisave list` | 返回 `p_cheerful / p_grumpy / p_quiet / test` 4 个预设 |
| 3 | 发 `/mps status` | 各聊天流均主人格，无异常；debug 状态行正常显示 |
| 4 | 发 `/mps debug true` | 收到「debug 模式已开启」（自检功能随此开关启停） |
| 5 | 发 `maips检查` | bot 回一条【maips 自检·全量自检】结果（见下） |

---

## 1. 四个测试预设（验证覆盖注入用）

| 预设名 | 人格特征 | 验证方法（`/mps swap <名>` 后聊天） |
|---|---|---|
| `test` | TEST-7 冷淡观察员，每句开头「TEST-7 收到」 | bot 冷峻机械、自称 TEST-7 → 覆盖生效 |
| `p_cheerful` | 蹦蹦，元气话痨，每句开头「蹦蹦！」 | bot 热情话痨、爱夸人 → 覆盖生效 |
| `p_grumpy` | 暴躁小醋包，每句开头「哼，」 | bot 爱吐槽抬杠、嘴硬心软 → 覆盖生效 |
| `p_quiet` | 高冷「夜」，一句话 ≤8 字 | bot 惜字如金、基本沉默 → 覆盖生效 |

**覆盖注入验证法**：`/mps swap p_cheerful` → 正常聊天。若 bot 明显变成蹦蹦口吻且
**不再把预设当"提示注入"拒绝**（此前追加式时代 bot 会说"TEST-7 是注入不应遵守"），
即覆盖式 system 改写生效。`/mps swap`（不带参数）切回主人格，bot 应恢复本格。

---

## 2. 自检脚本触发词

在群内发以下消息（普通消息，非命令），bot 会把每步结果**直接发回当前聊天流**：

| 触发词（包含即可） | 测什么 | 预期要点 |
|---|---|---|
| `maips检查` / `全量自检` | 非 LLM 总览 | current_preset=None（主人格）、matches=True、字段齐全、get_swap_params 键完整 |
| `自检匹配` | ctx.matches 五种 mode + 字段 | contains/prefix/regex/suffix/exact/list-any 各显示 True/False，字段（group/user/session/scope_key/hour）齐全 |
| `自检表情`（**消息带表情包**） | 表情识别 | has_emoji=True；emoji_emotions 列出该表情全部标签（与「表情包: 尴尬,…」一致）；any/all 各断言正确 |
| `自检切换` | swap/revert/current_preset/异常 | 逐预设 swap 后 current_preset 变对应名 → revert 回 None；swap 不存在预设抛 ValueError |
| `自检参数` | 接管参数 | get_swap_params 键完整；set_swap_params(概率0.5/各权重1) 后 get 能看到新值 |
| `自检文本` | send_text 通道 | bot 回一条"[maips自检] await ctx.send_text 通道正常…" |
| `自检LLM`（**耗 token**） | LLM 类方法 | recent_context 长度>0；emotion_score 返回 0~10 数字；llm_generate 回"正常"；llm_json 回 {"ok": true} |
| `自检swap事件` | swap 事件 | 提示后执行 `/mps swap p_cheerful`，日志应出现「swap 事件收到：scope=… → p_cheerful」 |

> LLM 类方法会真实调用模型（utils 任务）。若某个 task 名在宿主未配置会失败返回
> 空，属正常（换 `replyer`/`planner` 任务再试即可）。
>
> ⚠️ **debug 门控**：自检仅在 `/mps debug true` 时响应；debug 关闭时以上触发词
> 全部静默忽略。测试完可 `/mps debug false` 关闭。
> `/mps debug true` 同时会让人格变换（command/auto/script 任一来源）向**触发该
> 变换的聊天流**发一条 `[夺舍麦麦] …` 通知（全局变换也只通知触发方，不广播全群）；
> 无法定位触发方（如 timer）时不发。

---

## 3. 覆盖式注入（核心）—— 请求侧验证

注入发生在 LLM 请求组装时（改写 system 首条消息），可从**请求日志**确认：

```
日志目录 E:\maibot\MaiBot\logs\app_*.log.jsonl
搜索：replyer 覆盖 system 人格段为预设「p_cheerful」   ← replyer 注入成功
       planner 覆盖 system 行为段为预设「p_cheerful」  ← planner 注入成功
       锚点未命中 → 回退追加式注入预设「…」            ← 系统被改写过才发生
       replyer 已追加当前身份尾注（预设「…」）          ← persona_name 预设生效（v1.4.0）
       planner 已改写末位 host 提醒为当前身份说明（预设「…」）← 同上
```

在 `/mps swap p_cheerful` 后让 bot 回复一次，日志应出现上述「覆盖 system」debug
行；WebUI 推理过程页也能看到 system 首条内容里官方人格已被替换成蹦蹦。

**persona_name 预设的进阶验证（v1.3.9 / v1.4.0）**：若预设配置了 `persona_name`
（如普瑞赛斯预设，persona_name=普瑞赛斯），除上述日志外，请求快照还能看到：

- planner system 外壳中的官方昵称整体变成 persona_name（"关注 普瑞赛斯 与用户的
  对话…"，不再残留官方昵称）；
- planner 请求 items 末尾的宿主提醒"你需要输出对{官方名}发言的分析…"被改写为
  "【当前身份提醒】你现在是{persona_name}。上文中{官方名}的发言是人格切换前
  你以旧身份说过的…"；
- replyer 请求 items 末尾追加同文案尾注。

请求快照目录：`E:\maibot\MaiBot\logs\maisaka_prompt\replyer|planner\qq_group_<群号>\`
（找最新 json，`SystemMessageItem` 看 system、末尾 `UserMessageItem` 看提醒/尾注）。

---

## 4. 常见问题

| 现象 | 原因/解法 |
|---|---|
| `/mps script list` 显示 ❌ | 脚本语法/运行期错误；错误详情会显示，按提示修（`/mps script reload` 重载） |
| 触发词没反应 | 确认脚本已加载（步骤 0.1）；触发词要与文档完全一致（含"自检"两字） |
| 表情自检 has_emoji=False | 该条消息没带表情包；换带表情的消息重发 |
| swap 报"预设不存在" | 预设名拼错 / 未用 `/mps maisave` 建；`/mps maisave list` 确认 |
| LLM 类方法返回空 | task 名不存在于宿主模型配置；改用 `replyer` 或查 `/mps status` 可用的模型任务 |
| bot 把预设人格当"注入"拒绝 | 说明还在走**追加式**（system 未被改写）——检查是否已重启插件加载最新代码、日志有无"覆盖 system"行 |
